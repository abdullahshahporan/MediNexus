# MediNexus Assets

This directory contains assets for the MediNexus healthcare app.

## Directory Structure

- `/images/` - Image assets (logos, backgrounds, illustrations)
- `/icons/` - Custom icon assets
- `/lottie/` - Lottie animation files

## Adding Assets

Add your assets to the appropriate subdirectory. All assets are referenced in `pubspec.yaml`.
